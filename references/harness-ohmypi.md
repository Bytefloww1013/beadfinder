# Oh My Pi adapter

Copy pack to `.omp/skills/beadfinder/` and companions to `.omp/skills/{grill,research,to-spec}/`. Copy `adapters/ohmypi/agents/` to `.omp/agents/`.

Wayfinder: `autoloadSkills: [beadfinder]`, `spawns: architect,implementer,reviewer,product`.
HITL stays in the parent.
