# OpenCode adapter

Copy pack to `.opencode/skills/beadfinder/` and companions to `.opencode/skills/{grill,research,to-spec}/`. Copy `adapters/opencode/agents/` to `.opencode/agents/`.

Wayfinder `mode: all`. Workers `mode: subagent`. Reviewer and product `edit: deny`.
